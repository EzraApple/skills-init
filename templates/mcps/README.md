# MCP Placeholders

This directory is reserved for future project-local MCP setup.

The initializer does not install MCP servers yet. Keep future MCP descriptors
data-driven so profiles can opt into them the same way they opt into skills.
